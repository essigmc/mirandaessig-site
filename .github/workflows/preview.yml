name: Deploy PR preview (lower environment)

on:
  pull_request:
    types: [opened, reopened, synchronize, closed]

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: preview-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy-preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        if: github.event.action != 'closed'
        with:
          node-version: 20
          cache: npm
      - name: Install & build with preview base path
        if: github.event.action != 'closed'
        run: |
          npm install
          ASTRO_BASE="/pr-preview/pr-${{ github.event.number }}/" npm run build
      - name: Deploy / tear down preview
        uses: rossjrw/pr-preview-action@v1
        with:
          source-dir: ./dist/
