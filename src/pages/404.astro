---
import Base from '../layouts/Base.astro';
import { href } from '../utils/href.js';
---
<Base title="Not found — Miranda Essig">
  <h1>Page not found</h1>
  <p class="lead">That page doesn't exist (yet).</p>
  <p class="cta-row"><a class="btn" href={href('/')}>Back home</a></p>
</Base>
